//https://leetcode.com/problems/continuous-subarray-sum/description/

“The hash map stores the first occurrence of each prefix-sum remainder modulo k.
Since there are at most k distinct remainders and at most n indices, space complexity is O(min(n, k)).”

Time : o(n)

class Solution {
public:
    bool checkSubarraySum(vector<int>& nums, int k) 
    {
        unordered_map<int,int> umap;
        int n=nums.size();
        umap[0] = -1; //imp case 
        int sum=0;
        int remainder = 0;
        for(int i=0;i<n;i++)
        {
            sum += nums[i];

            //find remainder 
            remainder = sum % k;

            //check if in map
            if(umap.find(remainder) != umap.end())
            {
                //get size 
                if(i-umap[remainder] >= 2) return true;
            }
            else
            {
                //add to map
                umap[remainder] = i;
            }
        } 
        return false; 
    }
};
